<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
  </head>

  <body class="<%= controller.controller_name %>">
    <%= yield :layout %>
    <%= render "errors" %>
  </body>
</html>
