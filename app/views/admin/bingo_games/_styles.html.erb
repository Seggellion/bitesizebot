<style>
.highlight-pending #pending-actions-section {
  box-shadow: 0 0 25px 5px rgba(239, 68, 68, 0.4); /* Red Glow */
  border: 4px solid #ef4444;
  transform: scale(1.02);
}

/* Grayscale everything else slightly to draw the eye */
.highlight-pending .grid > div:not(#pending-actions-section) {
  opacity: 0.6;
  filter: grayscale(0.5);
}

/* Mini card specific styles */
.mini-card .grid-cols-5 {
  aspect-ratio: 1 / 1;
}

</style>